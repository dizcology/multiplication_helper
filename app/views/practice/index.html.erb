<p> 
  <%= parse(@item.content) %>
    
  <% if flash[:record] %>
    <p>Your answer <%= @record.event.response %> was <%= (@record.result)? "correct" : "incorrect" %>.</p>
    <% if !@record.result %>
      <p>The correct answer is <%= @item.answer %>.  </p>
    <% end %>
    <%= button_to "Okay! show me another problem.", :practice, method: "get", id: "another"%>
    <% flash[:record] = nil %>
    <% #TODO: add buttons to show solution(s)%>
  
  <% else %>
    <%= form_for @event, url: event_url(@event) do |f| %>
      
      <%= f.text_field :response, id: "response"%>
      <%= f.submit "Submit" %>
    <% end %>
  <% end %>

</p>

<script>
  $("#response").focus()
  $("#another").focus()
</script>
